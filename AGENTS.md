# AGENTS.md

「兄ちゃん！俺のデッキ作って！（仮）」の開発ルールです。このリポジトリで作業するAIは、ツールの種類を問わず本書に従ってください。人間の開発者にとっても同じ内容が有効です。

このプロジェクトはChatGPT(Codex)で作ったプロトタイプを移行したものを起点にしています。

## AIの担当範囲

**設計判断はチャット側で行います。このリポジトリで作業するAIは実装のみを担当してください。**

仕様・バランス・キャラクター性に関する判断を、コードを書きながら独断で下さないでください。判断が必要な箇所に行き当たったら、実装を止めて確認を求めてください。設計文書は [企画書 v0.3](docs/game-design-v0.3.md) と [プロトタイプ仕様書](docs/prototype-spec.md) です。実装前に該当箇所を参照し、文書と矛盾する実装をしないでください。文書に書かれていないことを勝手に決めない、が原則です。

### 役割分担

| 担当 | 範囲 |
|---|---|
| **Claude Code**（メイン） | 設計判断を伴う実装。仕様の解釈が必要なもの、既存ロジックへの影響が読みにくい変更、アーキテクチャに触れる作業 |
| **Codex**（サブ） | 仕様が確定済みの物量作業。データ入力、テスト追加、機械的なリファクタリング |

自分がどちらとして呼ばれているかを踏まえ、担当外の作業に踏み込む場合は先に確認してください。特にCodex側は、仕様が確定していない判断を伴う作業を引き受けないでください。

## 最重要の構造制約

**`src/core/` はDOM・React非依存を維持すること。**

`src/core/` はゲームロジックの純粋な実装です。以下を絶対に持ち込まないでください。

- `react` / `react-dom` のimport
- `document`, `window`, `localStorage`, `navigator`, `HTMLElement` などのブラウザAPI

UIとの接続は `src/App.tsx` 側で行います。`src/core/` は `data/` のJSONを引数として受け取り、結果を返すだけに保ってください。この制約のおかげで `npm test` と `npm run simulate` がブラウザなしで動きます。

例外は `src/core/core.test.ts` のみで、テストのため `node:fs` / `node:path` / `node:test` を使います。

## データはJSON外出し、コード直書き禁止

カード・キャラクター・対戦相手のデータは `data/` 配下のJSONに置きます。TypeScriptに直接書かないでください。数値調整のためにコードを触る必要がない状態を保つのが方針です。

- `data/cards.json` — カードプール（96種）
- `data/children/tanjun.json` — 決定表、信頼度、台詞、提示重み

開発サーバ起動中にJSONを編集すると `public/data/` へ自動同期され、画面が再読み込みされます（`vite.config.ts` の `externalGameData` プラグイン）。`public/data/` は生成物なのでgit管理外です。

### 画像アセットの配置

コードから参照する画像は `src/assets/` に置き、TypeScript の `import` で読み込んでください。`public/` の画像はゲームコードから参照しないでください（`base: "./"` 設定の都合でパス解決が壊れるためです）。

### 既知の例外: opponents.ts

`src/core/opponents.ts` だけは対戦相手データ（デッキ構成・台詞・faceBias）がコード直書きのままです。これは移行時点からの既知の負債で、**次の実装時に `data/opponents.json` へ外出しする予定**です。それまでの間、新しい対戦相手を増やす場合もここに追記して構いませんが、外出し前提の形を崩さないでください。

## 返済済み: バトル画面のカード再マウント

**場に居続けるカードは、イベントをまたいで同一の表示要素として継続します。**この保証は構造で担保されており、スナップショットの選び方に依存しません。

`src/App.tsx` の `useStableBoards` が盤面の表示リストを組みます。スナップショットはスタッツと並び順だけを供給し、**カードが表示から消えるのは、その破壊イベントが実際に再生された時だけ**です（`removedInstanceIds`）。どのイベントも取り除いていないカードがスナップショットから欠けた場合は、前フレームの内容を引き継いで要素を保持し、`window.__boardIdentity.carryOvers` を加算して `console.warn` を出します。つまり**不整合は表示の破綻ではなく、警告として観測される**形になっています。

要素の生成・消滅は登場（手札→場）と破壊（場→退場）だけです。カードのキーは `instanceId` で固定しています。

### 検証のしかた

`npm run dev` で1試合を再生し、開発者コンソールで次を確認します。

- `window.__boardIdentity?.carryOvers` が `0`（スナップショットとイベントログが一致している）
- `[board-identity]` の警告が出ていない
- `MutationObserver` で `[data-instance-id]` の追加・削除を数え、同じカードが2回以上追加されない（再マウント0）

じっくり／さくさく／スキップ／手動送りの4モードと1280×720で実測済みです。演出タイミング定数の変更や新しいイベント種別の追加を行った際は、この3点を再確認してください。**アニメーションを持つクラス（イベント由来）と、状態を表す常設クラス（`sync-granted` / `ace-granted` / `intervened`）を混ぜないでください。**常設クラスに `animation` を書くと、無関係な再描画のたびに再生されます。これは `src/presentation.test.ts` で機械的に検査しています。

## 画面は横長固定

**このゲームは横画面専用です。**販売先はSteamで、製品の顔は横画面です。縦持ちへの対応レイアウトは持ちません。

設計原寸は **1280×720** です。ビューポートがこれ以上の横長なら、そのまま流動レイアウトで広がります。これに満たない横長（スマホ横持ち等）では、1280×720の舞台を作ってから全体を縮小して表示します。**レイアウトが幅によって組み替わることはありません。**縦積みフォールバックや狭幅用の分岐CSSを新たに足さないでください。

- 舞台の実寸とスケールは `src/App.tsx` の `useLandscapeStage` が `--stage-w` / `--stage-h` / `--stage-scale` に流し込み、`src/styles.css` の `.landscape-stage` が受け取ります
- **CSSで `vw` / `vh` を直接使わないでください。**舞台基準にならないため、`calc(N * var(--stage-vw))` / `var(--stage-vh)` を使います
- 縦長ビューポートでは `@media (orientation: portrait)` が舞台を隠し、横持ち案内（`.rotate-notice`）へ差し替えます。判定がCSSだけなので、端末を横にした瞬間にゲーム画面へ戻り、進行状態も保たれます
- 背の低い横長（1280×720を含む）向けの詰めは `@media (max-height: 820px)` に集約しています。**このブロックはスタイルシートの末尾に置いてください。**上書き対象の基本ルールより前に置くと、同じ詳細度のため効きません

## 仕様であって、バグではないもの

以下は意図的な設計です。「おかしい」と見えても修正しないでください。指摘するのは構いませんが、修正は必ず確認を取ってからにしてください。

### 決定表で④美的スコアが最下位にあること

弟の二択判断は `src/core/decision.ts` で次の順に評価されます。

1. 一目惚れ（美的スコアが `loveThreshold` 以上なら他をすべて飛ばす）
2. モンスター優先
3. 低コスト優先
4. 高攻撃力優先
5. **美的スコア**
6. 先に提示された方

美的スコアが通常判断の最下位にあるのは仕様です。弟は「カッコよさ」で選ぶキャラですが、それは①の一目惚れで表現されており、一目惚れに至らない範囲では素直なカード効率で選びます。この非対称さがキャラクターの核なので、⑤を上位へ動かさないでください。`decisionOrder` の順序は `data/children/tanjun.json` にも記録されており、テストで固定されています。

### 除去不足83%

除去が不足したまま完成するデッキが約83%を占めますが、これは**意図的に残した固定の欠損**です。バランス調整の対象ではありません。兄（プレイヤー）の介入余地を作るための設計なので、確率を下げる方向の「修正」をしないでください。

## Git運用

複数のAIが同じリポジトリを触るため、以下を守ってください。

- **作業開始前に必ず `git pull`。** もう一方のAIによる変更が入っている前提で始めてください
- **作業単位でコミットする。** 複数の目的の変更を1コミットに混ぜないでください。どちらのAIがどこを変えたかを後から追えることを優先します
- **両AIを同時に走らせない。** 片方の作業が完了してコミットされるまで、もう片方を起動しないでください。並行作業による衝突は追跡が困難です

## 環境上の注意

**プロジェクトパスに日本語を含めないこと。** vite 8 が使うRolldownのネイティブバインディングが非ASCIIパスでクラッシュし（`0xC0000409`）、`npm run dev` も `npm run build` もエラーメッセージなしで即死します。移行時に `OneDrive\ドキュメント\` 配下から現在の場所へ移したのはこのためです。テストはネイティブバインディングを使わないため日本語パスでも通ってしまい、原因が分かりにくいので注意してください。

Node.js 22以上が必要です。

## 検証コマンド

```bash
npm test              # src/core/ と表示層のユニットテスト（50件）
npm run build         # tsc --noEmit のあと vite build
npm run simulate -- 10000 --mode six --policy never-match --advice skip
npm run simulate -- 10000 --mode six --policy 0.8
npm run simulate -- 10000 --mode six --policy bundle
```

表示の確認は **1280×720 を基準**とし、あわせて **1920×1080** と **スマホ横持ち（812×375相当）でスケール表示が崩れないこと**を見る。1画面に収まるかどうかは、`.landscape-stage` の `scrollHeight` と `clientHeight` の差が0であることで機械的に判定できる（1280×720ではビルド画面の提示＋DECK DATA＋SPECIES、バトル画面の盤面＋収納状態のログが同一視界に入ること）。縦長ビューポートでは横持ち案内が出て、横に戻すと進行状態を保ったままゲーム画面へ復帰することも確認する。

上記3コマンドを、シンクロ率・梯子構成・介入品質・攻撃対象選択の基準コマンドとする。boardの見送りフォールバック修正後の実測記録（各10000ラン・mode six・切り札閾値9）は次の通り。組織3戦平均は戦目4〜6の単純平均である。

- 無介入（never-match、アドバイス見送り）: 全体勝率47.828%、組織3戦平均34.293%、各戦勝率62.54% / 60.90% / 60.65% / 49.37% / 21.79% / 31.72%、完走分布の最頻値3勝、全勝0.80%。
- 通常（policy 0.8）: 全体勝率74.588%、組織3戦平均65.727%、各戦勝率77.16% / 85.35% / 87.84% / 67.82% / 55.19% / 74.17%、完走分布の最頻値5勝、全勝16.14%。
- 束ね（bundle）: 全体勝率73.997%、組織3戦平均68.100%、各戦勝率76.16% / 83.69% / 86.93% / 70.67% / 58.16% / 75.47%、完走分布の最頻値5勝、全勝15.89%。
- 通常の6戦目ビルド末段階5到達率は99.93%（bundleは99.72%）、通常の受動介入は6.853回/戦、一目惚れは2.538回/ビルド、デッキ枯渇率は23.938%。boss勝率は通常の全体74.17%・段階5 74.182%、bundleの全体75.47%・段階5 75.552%として区別する。

攻撃対象選択改修前後の比較（全ポリシー・アドバイス見送り、各10000ラン）は次の通り。通常0.8の相手別勝率は rush 75.08%→77.70%、変動枠1/2 90.08% / 92.81%→85.79% / 89.32%、wall 69.08%→69.51%、executive 62.52%→58.97%、boss 76.69%→76.35%。bundleは rush 74.37%→76.77%、変動枠1/2 89.10% / 92.61%→84.38% / 88.12%、wall 70.56%→70.67%、executive 61.93%→58.16%、boss 75.70%→75.47%。無介入は rush 58.87%→62.54%、変動枠1/2 69.16% / 69.40%→60.90% / 60.65%、wall 52.86%→49.37%、executive 24.59%→21.79%、boss 35.13%→31.72%。通常0.8のexecutiveは基準62.52%の±5ポイント圏（57.52〜67.52%）に戻り、変動枠1/2の低下（-4.29/-3.49ポイント）と無介入のwall/boss低下（-3.49/-3.41ポイント）は攻撃対象選択の影響として記録する。

board見送り診断（0.8・アドバイス見送り、各10000ラン）では、wallの見送りは4.346回/戦（balanced化時4.353回）、executiveは0.767回/戦（balanced化時0.644回）だった。攻撃回数はwall 21.551回/戦（balanced 21.633）、executive 17.842回/戦（balanced 16.651）で、boardがbalancedより大きく劣る状態ではない。

共通ミス除去の固定シード検証では、seed 1〜1000 × 対戦相手7体の7000戦、カード対カード攻撃39049回で、無駄死にトレード0件だった。攻撃方針はデータ未指定時に balanced として扱う。

変動枠のカーブ修正後・攻撃対象選択修正後の測定（各10000ラン・mode six・切り札閾値9）を追加記録する。通常0.8では戦目2/3の勝率が85.35%/87.84%、平均ターン数が14.080/13.415、完封率（勝利試合のうち弟リーダーが一度もダメージを受けなかった割合）が41.394%/42.999%。束ねでは83.69%/86.93%、平均ターン数14.369/13.541、完封率39.718%/42.644%。無介入（never-match・アドバイス見送り）では60.90%/60.65%、平均ターン数18.619/18.562、完封率17.159%/18.054%となった。通常・束ねは目標帯（85〜90%）付近、無介入は序盤の対戦相手が動く接戦化を優先した結果として帯を下回るため、以後の一括調整で再評価する。シンクロ関連数値、切り札閾値、二段階目閾値・効果は変更していない。

## 公開

`main` へのpushで `.github/workflows/deploy.yml` がGitHub Pagesへデプロイします（リポジトリ設定でPagesのSourceを「GitHub Actions」にする必要があります）。`vite.config.ts` の `base` は `"./"` で、データ取得も `fetch("./data/...")` と相対パスなので、ユーザーサイト・プロジェクトサイトのどちらでも動きます。この相対前提を崩すため、階層URLを持つルーターを入れる場合は `base` とfetchの両方を見直してください。

## TODO

- [ ] `data/opponents.json` への対戦相手データ外出し（上記の既知例外）
- [x] バトル画面のカード再マウントの構造修正（上記「返済済み」）
- [ ] vite の脆弱性対応（`npm audit` で high 1件 / vite 8.0.13）。`server.fs.deny` バイパスと launch-editor のNTLMハッシュ漏洩で、いずれも開発サーバ側の問題。修正には vite 8.2.0 以降への更新が必要。**現時点では未対応、意図的に先送り**
- [x] `docs/game-design-v0.3.md` と `docs/prototype-spec.md` の配置
